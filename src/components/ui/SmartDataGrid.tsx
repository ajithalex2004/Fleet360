/**
 * @deprecated Prefer `FleetDataGrid` with gridName="RoutesGrid" etc.
 * Kept so existing `import SmartDataGrid from '@/components/ui/SmartDataGrid'` keeps working.
 */
export { default } from './FleetDataGrid';
export type { DataGridColumn, SortDir } from './FleetDataGrid';
